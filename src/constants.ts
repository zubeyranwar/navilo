export const DEFAULT_OPTIONS = {
    pagesDir: 'src/app',
    typescript: true,
};

export const VIRTUAL_ROUTE_MODULE_ID = 'virtual:preluder-routes';
export const RESOLVED_VIRTUAL_ROUTE_MODULE_ID = '\0' + VIRTUAL_ROUTE_MODULE_ID;